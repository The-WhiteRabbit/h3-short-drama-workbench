# Tudou Studio API

## GET /api/health

Check local service status.

## GET /api/project

Returns current project information and scanned assets.

## GET /api/assets

Returns asset list with SHA256 fingerprints.

Future:

- POST /api/review/script
- POST /api/review/storyboard
- POST /api/review/clip
- GET /media/{path}
