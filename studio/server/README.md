# Local Studio Server

Responsibilities:

- Scan project folders.
- Return current manifest state.
- Serve media with local range support.
- Record review decisions.
- Detect changed assets and invalidate stale approvals.

The server is intentionally local-only. It is not a production web service.
