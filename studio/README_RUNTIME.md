# Tudou Studio Runtime

Run:

```bash
python studio/run.py
```

Runtime flow:

script -> storyboard -> H3 jobs -> candidates -> review -> selected clips -> timeline export

Human approval remains required between stages.
